name: Keep Render Services Alive

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl https://ai-knowledge-backend-7rwq.onrender.com/api/health

      - name: Ping worker
        run: curl https://ai-knowledge-worker-msrb.onrender.com/health
